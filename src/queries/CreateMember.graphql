mutation CreateMember($firstName: String!, $lastName: String!, $email: String!) {
  createMember(
    input: {
      member: {
        firstName: $firstName
        lastName: $lastName
        email: $email  # <-- now added
      }
    }
  ) {
    member {
      id
      firstName
      lastName
      email
    }
  }
}
